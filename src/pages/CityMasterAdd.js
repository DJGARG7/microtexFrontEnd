import React from "react";
import SideBar from "../components/Admin_components/SideBar";
import { CityMasterData } from "../jsonData/CityMasterData";
function CityMasterAdd(props) {
    return (
        <div>
            <h1>This is city Master Add</h1>
            {/* <SideBar SideBarData={CityMasterData} userDetails={props.userDetails} /> */}
        </div>
    );
}

export default CityMasterAdd;
